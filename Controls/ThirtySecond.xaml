<UserControl x:Class="CoordinationTraining.Controls.ThirtySecond"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CoordinationTraining.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="{StaticResource TactSizeHeight}" d:DesignWidth="360"
             MaxHeight="{StaticResource TactSizeHeight}" MaxWidth="360"
             MinHeight="{StaticResource TactSizeHeight}" MinWidth="360">
    <Grid>
        <Rectangle Grid.Row="1" Grid.ColumnSpan="4" Height="6"  VerticalAlignment="Top" Fill="{DynamicResource HiHat_CB}" Margin="43, 0"/>
        <Rectangle Grid.Row="1" Grid.ColumnSpan="4" Height="6"  VerticalAlignment="Top" Fill="{DynamicResource HiHat_CB}" Margin="43, 15, 43, 0"/>
        <Rectangle Grid.Row="1" Grid.ColumnSpan="4" Height="6"  VerticalAlignment="Top" Fill="{DynamicResource HiHat_CB}" Margin="43, 30, 43, 0"/>
        <StackPanel Orientation="Horizontal">
            <local:Fourth/>
            <local:Fourth/>
            <local:Fourth/>
            <local:Fourth/>
        </StackPanel>
    </Grid>
</UserControl>
